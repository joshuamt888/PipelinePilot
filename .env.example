# SteadyLeadTracker Environment Variables
# Copy this file to .env for local development and fill in your actual values
# DO NOT put real secrets in this file - this is just a template!

# Database Configuration (PostgreSQL)
# For local: postgresql://username:password@localhost:5432/steadyleadtracker
# For Railway: Railway auto-generates this as DATABASE_URL
DATABASE_URL=postgresql://localhost:5432/steadyleadtracker

# JWT Secret for authentication 
# Generate a long random string - NEVER use this example value!
JWT_SECRET=REPLACE-WITH-LONG-RANDOM-SECRET-KEY-MINIMUM-32-CHARACTERS

# Session Secret for express sessions
# Generate another random string - NEVER use this example value!
SESSION_SECRET=REPLACE-WITH-ANOTHER-RANDOM-SECRET-KEY

# Server Configuration
NODE_ENV=development
PORT=3000

# Optional: Email Configuration (for future password reset feature)
# EMAIL_SERVICE_API_KEY=your-email-api-key-here
# EMAIL_FROM=noreply@steadyleadtracker.com

# Optional: Security Configuration
# CORS_ORIGIN=http://localhost:3000